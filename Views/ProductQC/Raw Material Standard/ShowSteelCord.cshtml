@model AspnetCoreMvcFull.ModelDTO.Product.RawMaterialSteelCordDTO

@if (Model != null)
{
    <img style="width:100%;"
         src="@Url.Content("~/images/" + Model.image)"
         alt="Ảnh hướng dẫn" />
}
