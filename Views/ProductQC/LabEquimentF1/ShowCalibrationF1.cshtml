@model AspnetCoreMvcFull.ModelDTO.Product.LabCalibrationF1DTO

@if (Model != null)
{
    <img style="width:100%;"
         src="@Url.Content("~/images/" + Model.image)"
         alt="Alternate Text" />
}
