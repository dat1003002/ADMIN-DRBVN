@model AspnetCoreMvcFull.ModelDTO.Product.LaboratoryEquipmentF1DTO

@if (Model != null)

{
    <img style="width:100%;"
         src="@Url.Content("~/images/" + Model.image)"
         alt="Alternate Text" />
}
